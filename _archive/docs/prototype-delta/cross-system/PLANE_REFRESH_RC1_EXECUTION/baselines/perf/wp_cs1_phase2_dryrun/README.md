# WP-CS1 Phase 2 Harness Dry-Run

Artifacts produced by `scripts/run_rc1_harness.py steady-train --device cpu --epochs 1 --batch-size 2 --disable-compile` (2025-10-03).

These serve as shape validation for the Phase 2 harness implementation (JSON schema + aggregated `summary.csv`).

Run on CPU-only node (torch.cuda.is_available() == False).
