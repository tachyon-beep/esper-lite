# Metrics — Esper             Last read: 2026-07-11

> Research framework, not a commercial product: the "north-star" is the morphogenetic
> objective J (committed per-param counterfactual gain), not an engagement metric.
> BASELINE values below are real session readings; TARGET numbers/dates are placeholders
> for the owner to set (a target with no number+date is not falsifiable — do not accept).

## North-star
| Metric | Target (falsifiable) | Current (BASELINE) | Read on | Trend |
|--------|----------------------|--------------------|---------|-------|
| **Committed J** = Σ_seed (1/params)·Σ_t cf·α over committed (FOSSILIZED) residency | ≥ placebo-noise-floor + TARGET by `<owner-set date>` | **~0 on control (defect under repair); fossilize/ep 0.207±0.006** | 2026-06-25 (n=5 control) | → |

## Input metrics (the levers that move the north-star)
| Metric | Target | Current | Read on |
|--------|--------|---------|---------|
| corr(reward, J) — reward aligned to the J yardstick (EPISODE-level: episode-summed reward vs episode J) | **THE A/B effect-size floor (PDR-0019): paired Δcorr ≥ +0.10 absolute at n=5; LB > 0 at n=10.** Primary gates = design criteria (i)–(v); the Δfossilize_payable_J overlay is superseded (structurally zero median); step-level is NOT the gate (F2 sampling ceiling) | **A/B SCORED (PDR-0026): mean paired Δcorr = −0.017, 2/5 positive → floor FAIL = NULL-NOT-INFORMATIVE (asymmetric null; paid episodes 0.49%, ΔSD 0.066 = pure-noise prediction).** OFF-arm replication 0.214 validates the estimator (baseline 0.212 ± 0.046) | 2026-07-05 (n=5 A/B) |
| **Placebo noise floor tau** = P99 of SIGNED terminal (φ − c_paid) for a null player — the north-star's "placebo-noise-floor" term and the enablement deadband | **OWNER-RATIFIED (PDR-0018): ACCEPT-PROVISIONAL / LOWER-BOUND ONLY** — sufficient to open F2 calibration; NOT the final ON operating threshold; ON-run recalibration MANDATORY before any n=10 magnitude claim | **tau = +0.28 pp (lower bound). Full 2-arm, 3240 samples/arm: 1e-3 arm +0.2401 CI [+0.2398, +0.2721]; 1e-4 arm +0.1201 CI [+0.1000, +0.1401]. Non-degeneracy PASSED. ⚠ plateau trigger FIRED (PDR-0015): floor is magnitude-dependent — hence lower-bound-only status** | 2026-07-03 (ratified) |
| **A/B coverage** — share of positive pre-fossil value mass reachable by the fossilize-only payment gate (J currency, param-normalized) | pre-registered rule (gate comment #99): <50% blocker, 50–75% yellow, >75% licensed | **PREDICTION CONFIRMED EXACTLY (PDR-0026): observed k≥2 = 132/12000 = 1.10% (predicted 1.1%); paid 59/12000 = 0.49% (~12/run predicted, 8–16 observed); P(pay\|k≥2) 44.7% vs 45.4% factorial prior.** The coverage ceiling — not the credit math — is what nulled the direction read | 2026-07-05 (n=5 A/B) |
| share_attribution — reward variance carried by the cf-attribution term | context only (magnitude-confounded: `ba`≈68% of |reward|) | 0.925 ± 0.004 | 2026-06-25 (n=5) |
| **EV Stage-0 gate** — `Cov(R_cf, R)/Var(R)` on a Stage-2-**OFF** control run (value-free, raw-scale, per-return decomposition) — THE Now-bet metric | **`> 0.40` ⇒ cf stream dominates return variance ⇒ Stage-2 de-shaping justified; AND low `r_main_var_share = Var(R_main)/Var(R)` (the smoothness leg)** | **PASSED (PDR-0032; confirmed on the fuller read PDR-0033): median `Cov(R_cf,R)/Var(R)` = 1.017, IQR [1.006, 1.033], **41/42 cf-active updates > 0.40** (42 updates, ep 4→172; min 0.10, max 1.16; residual ~1e-17 — partition reconciles on LIVE data). `r_main_var_share` ≈ 0 (R_cf carries ~all return variance). **FLAT at ~1.0 across the whole observed window** (random-init → epoch 21 / ~50% acc) — no decay as the host matures (single sub-0.40 reading = one early transient, ep 20). PROVENANCE: control run **terminated externally at 172/400 (~43%)**; the leak-in-new-telemetry hypothesis was investigated and **RULED OUT** (PDR-0033 — pre-allocated SoA, flat batch cadence, 41 GiB free / no OOM). Reading is on the PARTIAL run, not the full 400; **owner ACCEPTED the partial read as the gate result** (formal full-400 rerun offered as optional closure, declined — 2.5× the threshold, flat, IQR ~0.03). Stage-0 task esper-lite-3d67b09687 CLOSED. Instrument on `feat/ev-stab-stage2-hra` (5 commits `562cb5bf`→`70753923`, ~2400 tests green, byte-identity direct).** | 2026-07-05 (partial control run, 172/400) |
| **Stage-2 MAJOR-1 acceptance gate** — paired fresh-init HRA-ON/OFF composite verdict (the acceptance test for the Now bet's next leg) | **ACCEPT ⟺ (LEG-A `ev_sum` non-inferior: median Δ ≥ −δ, δ frozen from OFF) ∧ (LEG-B advantage-path volatility reduction: `IQR_u(√(1−ev))` ↓ ≥ ε_rel=0.10) ∧ MECH(ev_main vol ↓ ≥⌈0.8n⌉) ∧ safety(val-acc ≥ −0.3pp, params, churn, guard-channels) — banked ONLY at n=10.** Pre-registered, falsifiable, frozen before data | **NOT YET READ. Harness = pure scorer (PDR-0037) + wrapper pure layer (PDR-0039) + S5/S6/S7 (PDR-0040/0041/0042) + review-remediation checkpoint (PDR-0043, latest code commit `f0ee27fe`) + final-review closeout `3669934d` (PDR-0044; task esper-lite-2a4b56e719 closed). Escrow telescoping correctness fixed in `89b50a16` (PDR-0047): reward-path escrow delta clipping removed; Obs V3 schema v2 exposes stable accuracy and escrow-credit state for the critic. Known review findings fixed: telemetry-derived G2 `host_params`, terminal per-env evidence coverage, duplicate `runs` row rejection, fail-loud required-field completeness, S7 diagnostic scalar plumbing, fossilize churn in G3, leyline boundary routing, return-variance denominator diagnostics, OFF calibration validity, spec-scoped traceback/run-log validity, and `advantage_std_floored` reporting. **FORMULAS FROZEN 2026-07-08 (PDR-0048, owner-ratified after drl-expert review):** `g3_ratio_max=1.5` per-channel, `g4_ratio_max=2.0`/`g4_abs_floor=2`, `floored_asymmetry_max=0.10` per-pair, `Δparam_max`/`W`/δ resolve from OFF arms at §10 step 3 via pre-committed formulas; ε_rel=0.10 gets a step-3 OFF-spread validation; budget = 200 rounds/run, min 150 scored updates; posture = prior-A/B base + rvt ON (`configs/ablations/stage2-ab-{off,on}.json`). **OFF wave COMPLETE 2026-07-09 — 5/5 valid** (200/200 updates each, 0 skips, ~6h/run; first wave killed by session reap 16:04 2026-07-08, relaunched session-proof @ `fe177844`, PDR-0051). **STEP-3 FROZEN (PDR-0050, gate §11.1): W=17, δ=0.050311, Δparam_max=1527.8; G3 spread PASS (≤1.379).** Two pre-registered escalations FIRED and were owner-ruled same day: ε_rel validation FAIL (OFF adv-vol spread 0.5716 ≥ 0.10) + material non-stationarity (2nd/1st-half vol ratios 2.4–13.6×) → **LEG-B DEMOTED TO DESCRIPTIVE at the n=5 screen; screen predicate = LEG-A ∧ MECH ∧ G1–G4** (no retro-promotion; volatility claim deferred to n=10 / redesigned metric). Device placement ruled provenance-not-frozen-config (harness amended read-path-only, 182 tests green). OFF calibration facts: ev levels 0.132/0.114/0.194/0.045/0.282; ev IQRs 0.257–0.468. **§11.2 amendment (PDR-0052, 2026-07-10):** the cf_value_loss plateau VALIDITY gate proved unsatisfiable on real ON telemetry (cf target scale non-stationary — same cause as LEG-B) → demoted to DESCRIPTIVE; retained hard = cf presence (both-side signature) + finiteness; §11.2 descriptive block now computed + rendered in the packet. Acceptance reader hardened per high-effort review (outcome bounds from leyline, duplicate/NULL guards, coverage row counts). **First ON wave (23:33 2026-07-09) DEGRADED — telemetry writer starved by co-tenant review workload (dropped events; 199/200 updates, 2392/2400 outcomes on s41); operational fault, not code (PDR-0056); quarantined `degraded-cotenancy-1/`. Full 5-seed RERUN launched 05:52 2026-07-10 @ `a0ad968e` (training tree byte-equivalent to `fe177844`; epoch-1 bit-identical). Quiet-box rule now standing.** **READ 2026-07-11: REJECT at the n=5 screen (PDR-0059; packet `docs/analysis/2026-07-11-stage2-major1-packet.txt` @ `bac4cc7f`).** Rerun 5/5 clean (5× rc=0, zero drops — PDR-0056 trigger never fired). First scoring INVALID on 2/2000 emitter NaN-markers checked pre-§8B → owner-ruled §11.3 validity-envelope correction (Option B, PDR-0058; scorer fixed+tested `d187402a`, inputs hashed, no-peek held, single rescore) → §1 VALID (floored fractions 0.5% on exactly the two known rows). **REJECT fired on: LEG-A FAIL (per-seed Δ_A −0.1022/−0.0876/−0.0978/+0.1742/−0.0928, median ≈ −0.093 < −δ=−0.0503; 4/5 seeds regress) ∧ MECH fail (ev_main vol not stabilized on ≥⌈0.8n⌉ seeds) ∧ G4 fail (gradient_pathology all ON seeds 4–7 vs OFF 0–9 incl. zeros; s42 rollbacks 34 vs 3; NOTE value_collapse ON 0–2 vs OFF 3–11, ON better). G1 PASS (val-acc within −0.3pp), G2/G3 hold. LEG-B descriptive: INCONCLUSIVE (4/5 reduce but confound downgrade fired; s44 outlier both legs +0.174/+0.555).** Descriptive: cf target scale ~14–18 vs main ~4–5, Q1→Q4 drift ~2–4→~24–28 every seed (cf-stream non-stationarity, same root as LEG-B/§11.2). Mediator caveat recorded (estimates HRA under current entropy-floor objective + 200-round schedule). **Pre-committed: NO n=10 for objective-A HRA as implemented; Stage-0 diagnosis untouched; epic direction = owner DECIDE.** | **READ 2026-07-11 = REJECT (n=5 screen)** |

## Guardrails (must NOT degrade)
| Metric | Floor / ceiling | Current | Read on |
|--------|-----------------|---------|---------|
| Host-accuracy contribution (all-seeds-on − all-disabled) | ≥ floor `<owner-set>` (must stay positive) | **+7.69pp mean, n=6 (host-alone ~38% → with-structure ~46%; per-run +6.67…+8.27, tight) — UPGRADES the prior n=1 +6.1%; morphogenesis confirmed REAL** | 2026-06-30 (3 control + 3 suppress) |
| **Policy DECISION-STEP (conditional) entropy** | must NOT collapse (≳ 0.1 on decision steps) | **HEALTHY — slot 1.0→0.30–0.70, op ~0.88 (never <0.1). Prior "⚠ BREACHED" was a FALSE ALARM (PDR-0006): the alarm read `head_*_entropy` = learnable_fraction × conditional — a density proxy diluted by ~60% forced steps; the ~411 anomalies were a telemetry artifact. Raw series → relabel `head_*_entropy_density`.** | 2026-07-01 |
| Rent / efficiency — committed param & compute cost per acc-point | ≤ ceiling `<owner-set>` | **n=5 BANKED (5/5 seeds): control 12–20 vs suppress(no r0c0) 7–9 pp/M-param, median Δeff −7.0 (sign-test p≈0.03) ⇒ r0c0 is an efficiency-enabling stem (b), NOT a freeloader (PDR-0009). DIRECTION banked; MAGNITUDE → owner's n=10 call (the "CI" is a sign test, not a CI). [prior n=1: +252% params / 7× compute for +6% acc]** | 2026-07-02 (n=5 J-read) |
| Churn not reward-farmed — germinate/prune per episode vs contribution | not decoupled from contribution | germ 12.4 / prune 11.6 per ep | 2026-06-25 (n=5) |
| **ON-wave mechanism validity (COMPLETE, 5/5 banked)** — TOPUP finiteness, G1 trips, anomaly rates vs OFF baseline | zero non-finite; G1 zero trips; anomaly rates ≤ OFF baseline | **ALL GREEN (PDR-0026): 2421 TOPUP all finite, tau stamped 100%, zero deadband violations (0/2554), G1 0 trips, G2/G4 clear, final-acc median Δ −0.12pp (≥ −0.3 gate). G4 flag RESOLVED — reference-class error (44.7% vs 45.4% design prior; ~1% was the placebo-null rate). Secondary note: governor rollbacks ON 12.4 vs OFF 7.4 mean/run (s43=21) — no gate, RCA if it recurs in a future ON leg** | 2026-07-05 (scored) |
