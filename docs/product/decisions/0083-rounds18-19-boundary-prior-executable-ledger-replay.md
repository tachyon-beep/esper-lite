# PDR-0083 — Rounds 18–19: the audit's own F3 fix rejected (boundary-home instead); premium SETTLED at 1.464538 boundary-paid; F5 reclassified pending telescoping; NEW blocker = executable G-LEDGER replay

Date: 2026-07-14   Status: accepted (both primes converged; claude-prime self-retracted its round-19 first draft's over-reads — the corrected form is what's integrated)
Follows PDR-0082. Artifacts: audit addendum A1–A6 in `docs/analysis/2026-07-14-fossilize-reward-path-audit.md`; pre-registration body reconciled to Groups-A–D altitude (§1.1/§1.4/§1.6/§1.8/§2.3–2.8/§1.13/§3.5).

## What does this buy?  (REQUIRED — PDR-0068)
It catches the audit's OWN recommended fix violating the invariant it cited (a `q_decision`-gated immediate payment is policy-timeable — the fifth mis-read in the arc's money path), replaces it with a construction that is compliant by design (boundary payment on `q_settle`), and converts the ledger from hand arithmetic to an executable certification. Measured: the request instant now carries zero contribution-conditioned payments of either sign; freeze blocker #2 is restated as a replay harness with two numeric invariants instead of a scalar.

## Decisions
1. **Premium SETTLED (both primes): `1.464538 × legitimacy_request`** — the matched-control consolidation of both shipping flat terms (0.5 base + 3·tanh(1/3) ≈ 0.9645). Paid at the **settlement boundary**, **discount-neutral** (`÷γ^d`, request→boundary delay d; index convention fixed by unit test — else a 5–14-epoch window silently shrinks the prior's PV), **qualified on the frozen predicate `q_settle ≥ 1.0`**, `legitimacy` frozen at request, never retuned from results. "Amount-matched, boundary-qualified fixed commitment prior." Formal owner signature lands with the freeze.
2. **F3's `q_decision` re-key REJECTED** (the audit's own recommendation): `q_decision` is available at the request instant by definition → a 1.464-unit reward cliff gated on it is a threshold instrument on a smoothed observable (EWMA-crossing timing — READ 1's falsified class, one level up). An action-mask form would also mechanically bake the quote→request correlation into P2. **Request-instant sterilization instead:** at request, NO contribution-conditioned payment of any sign (0.1·c grading, spot-c gate, −0.2 branch, counterfactual-graded penalties all move to the boundary keyed on `q_settle`); only `fossilize_cost=−0.01` charges at request.
3. **F5 RECLASSIFIED — code fact, economic classification OPEN.** PBRS with a state-function potential should telescope (`γ^T·Φ(s_T) − Φ(s_0)`); the truncation residual is non-negligible (`γ^150 ≈ 0.47`, `Φ_FOSS=6.0` vs `Φ_PRUNED=0` — possibly net pro-fossil). No numeric refund; DEFINE the PENDING state's potential, then verify whole-trajectory cancellation in the replay. F7 demoted to "illustrative fixed-term balance at dwell 5" — no dominance claims from any single scalar.
4. **`holding_warning` window condition READ ANSWERED** (`contribution.py:706-717`): fires only when the policy targets the pending slot with a non-terminal op (per-decision reward), so round-19's first-draft −1.5 was an over-read (self-retracted); magnitude = −0.1…−0.3 × P(target pending slot). Still unpriced → **spec clause: suspended for `committed=True` seeds** (the request ends indecision; pending state semantically distinct in reward logic).
5. **NEW FREEZE BLOCKER — executable G-LEDGER replay** (addendum A6): production SHAPED reward path over the 9-dimension case matrix, per-component discounted PV, two invariants — (a) accounting continuity ≈ 0 (~1e-6 float64) excluding the designed prior + genuine costs, for +/0/− constant c; (b) designed transaction delta = exactly the frozen terms, invariant to dwell/phase/spot/noise/PBRS-path/warning-firing — plus the event-order assertion. Hand arithmetic found the terms; only the replay certifies transition semantics.
6. **Pre-registration body reconciliation DONE** (was gpt's blocker list): two-quote §2.3, signed-continuity §2.5.2 (negative `q_settle` → negative annuity; `None` ≠ 0), masked late requests §2.5.6, request sterilization §2.5.4, third-regime window ledger §2.5.9, executable invariants §2.6, state semantics §2.7, per-run P2 §1.6, protocol-package estimand §1.1/§1.4, rounds-15–17 verdict taxonomy §1.8. No stale clauses remain for the frozen artefact.

## Process note (banked)
Round 19's first draft asserted a sign-flipping magnitude from an unread input and marked a second-order compliance point ⛔-fatal — then self-retracted both ("asserting-with-arithmetic what should have been flagged-with-a-grep"). The working rule it produced is adopted: **severity markers are reserved for executed reads; unread conditions enter as read-queue items with the grep named.** This matches [[instrument-validity-before-interpretation]].

## Freeze blockers now
(1) executable G-LEDGER replay (A6); (2) paired-run power calc (P2 + GATE-DOM + GATE-INFL, blinded reassessment, frozen max-n); (3) one final adversarial pass. Premium/body/audit blockers are closed. **No GPU.**

## Reversal trigger
- If the replay shows PBRS does NOT telescope (clips/terminal handling/PENDING state) → the residual is a genuine protocol incentive; it enters G-LEDGER's designed-delta budget explicitly, and the PENDING potential is redesigned (never numerically rebated).
- If the replay's accounting-continuity invariant fails for negative constant c → the signed-continuity transform is mis-specified; the settlement does not land until it passes.
- If the power calc cannot bound the gates at the frozen max-n → GATE-DOM demotes to descriptive at screen ONLY with the pre-registered statement that the dominated-commitment case is live at screen (PR26).
