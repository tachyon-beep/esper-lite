# PDR-0078 — Gating reads resolved: commitment is CAUSALLY good (founding premise vindicated); reward permanence but on a settlement instant the policy cannot select

Date: 2026-07-14   Status: accepted (zero-GPU reads on r9; the founding-premise finding is the strongest causal read in the epic — exact identification, known-constant propensity, balance-checked, MDE-gated)
Related: PDR-0074 (the measurement gap), PDR-0076/0077 (refocus + L2 block), `docs/analysis/2026-07-14-permanence-visibility-primes-review-pack.md`, round-13/13.5 primes. Read scripts durable in the session scratchpad (`read{1,2}_out.txt`, `read3_v2.py`, `read3_v2_out.txt`).

## What does this buy?  (REQUIRED — PDR-0068)
It answers the question the whole epic exists to answer — is under-fossilization a real defect — with a proxy-free causal read, and it fixes the settlement design direction with evidence. Measured: a well-powered (2–5× MDE) local ATE of forced FOSSILIZE on terminal host accuracy, both seeds; and an oracle-timing farm bound that rules out every policy-priced settlement.

## The three reads
- **READ 1 (oracle-timing farm replay): every policy-timed settlement is farmable → A[ewma] BLOCKED.** Each rule leaving the commit instant under policy control has a material, OOS-persistent, `c_fair`-invariant timing-selection farm (16–52% of fair; safest α=0.2 still material OOS). This is a LOWER BOUND (a farm-trained policy would learn to dwell in HOLDING to manufacture commit times, unsimulable offline). Only a **scheduled/terminal/non-cancellable-confirmation-window** instant removes the max-operator by construction.
- **READ 2 (commitment-hazard null): no spike-selection, but UNINFORMATIVE about post-fix risk.** Hazard OR ≈ 1.0 (CI straddles 1); the "45–47%" is the opportunity-set base rate. The current policy isn't gaming only because it is floor-forced (dead-zone) — a gradient trained against A[ewma] would move from this null toward the READ-1 farm. A null here measures the dead-zone, not safety.
- **READ 3 (RE-RUN, exact |H|=1 identification + IPW/MSM): DECISIVE — commitment is CAUSALLY good.** `op_entropy`'s discrete grid recovers |H|=1 and `num_valid` EXACTLY (concave entropy minimized only at the pinned-simplex vertex; PR15 three-way cross-validation passes: op_entropy on-grid 74.8%/81.5%, action_confidence ∈{0.15,0.55} exactly, op-mix matches). Floored ops have a **known constant propensity 0.15** → MSM/IPW (no propensity model, no positivity). Collider-safe design A (FIRST |H|=1 decision/episode). Randomization balance-checked (all |SMD|<0.08). MDE-gated. **Forced FOSSILIZE vs WAIT causally improves proxy-free host outcomes:** terminal `final_accuracy` **+1.4 to +1.6 pp** [CIs exclude 0], val-acc@{1,5,10,25} +1.1 to +2.0 pp, host-accuracy AUC +1.5 to +1.7, **no increase in destructive events** (a decrease on s42). Well-powered (2–5× MDE, d≈0.2–0.7), reproduced across both seeds and both designs (A collider-safe + B all-decisions). **The pre-commit EWMA quote is a VALID proxy** (CATE monotone in q: s41 +1.19→+1.69→+1.87; s42 +1.01→+1.12→+2.34).

## The call / resolution
1. **The founding premise is SUPPORTED — permanence carries real, proxy-free product value the reward zeroes out — at the honest altitude of the contrast (advisor calibration).** What is measured, decisively: **committing a floor-pinned good HOLDING seed NOW causally beats WAITING (deferring) this step** — +1.4–1.6 pp terminal accuracy, well-powered, both seeds, no added destruction (less on s42), rising with the pre-commit quote. That the reward can't see this value, and taught the policy permanence is worthless, is the right story, and the fix is worth building. **OPEN (do not bank "under-fossilization is a defect, full stop"):** (i) forced-WAIT ≠ never-commit (the seed stays HOLDING, can commit later) → this is **commit-EARLIER vs commit-more**, both consistent with +1.5 pp; (ii) the clean contrast is FOSS-vs-WAIT, NOT FOSS-vs-SET_ALPHA (the policy's actual dominant alternative, excluded as confounded) → "should commit *more* than it does" is not what was measured; (iii) the s42 prune-count DECREASE hints part of the gain is **permanence shielding a good seed from the floor's own later forced-PRUNE**, not intrinsic host benefit. **One free r9 read settles all three:** decompose the WAIT arm by later-force-pruned vs not — if the effect concentrates in WAIT-then-lost seeds, reframe to "locking in good seeds protects realized value that provisional status puts at risk" (still epic-vindicating; sharpens the settlement to reward *protecting realized value*). Fold this into the "mine r9 first" batch — it dies the moment K>1/v4 runs.
2. **Reward permanence, but on a NON-SELECTABLE settlement instant** (fixed-time / terminal / non-cancellable REQUEST_FOSSILIZE confirmation window). NOT a policy-priced quote (READ 1). A[ewma] and all policy-timed rules stay BLOCKED. The EWMA quote is a valid VALUE proxy (READ 3) but must not be evaluated at a policy-chosen instant.
3. **L1 (obs legibility) + K>1 remain the correct-regardless lands** (diffs done, under review). The floor dead-zone stays deferred + coupled.

## Scope caveats (banked — do not over-read)
- Local ATE on the **floor-pinned |H|=1 subpopulation**, **FOSS-vs-WAIT** contrast — "commit-now vs wait," not "morphogenesis vs nothing" (no no-morphogenesis control arm; `final_accuracy` exists but the control does not). It measures the effect of committing the seeds the CURRENT (floored) process produced.
- Measured on r9 (K=1/obs-v3 regime). Proxy-FREE on the outcome side (terminal accuracy), which is the point.

## Reversal trigger
- If a no-morphogenesis / all-seeds-off control (L3-lite) shows the +1.5 pp is not attributable to the commitment decision (e.g. waiting-then-committing-later captures the same gain) → the "commit NOW" urgency softens (the value is in the seed, not the timing).
- If the non-selectable settlement instant reintroduces a farm via a different channel (e.g. dwell-to-inflate-the-terminal-quote) → revisit the settlement design.
- If a v4/K>1 rerun fails to reproduce the |H|=1 causal sign under the new regime → the r9 finding was regime-specific.
