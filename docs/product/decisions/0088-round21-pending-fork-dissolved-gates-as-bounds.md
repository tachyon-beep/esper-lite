# PDR-0088 — Round-21 adjudication: Φ(PENDING) fork PROPOSED-DISSOLVED (flag-on-HOLDING); shipping PBRS reframed as a non-telescoping progress stream; gates re-postured as BOUNDS; conventions pinned

Date: 2026-07-14   Status: accepted (adjudication + fold-in within standing authority; the DISSOLUTION, the gates-as-bounds posture, and price-don't-fix remain OWNER-RATIFIED at freeze)
Follows PDR-0085/0086/0087. Inputs: claude-prime round-21 + gpt-prime round-21 (owner-relayed), adjudicated against the phase-1 harness before banking. Artifacts: pre-reg header/§2.5.9/§2.6/§ROUND-21/blockers updated; audit A8 appended.

## What does this buy?  (REQUIRED — PDR-0068)
It removes a wrong owner decision from the queue instead of asking the owner to make it: both recorded Φ(PENDING) options are dead under executed facts (the default's parity premise is false; the alternative double-mints a ≈+0.52 climb), and the spec's own §2.1 wording already contains the correct construction. It also converts two power-calc §8 questions from judgment calls into arithmetic consequences, and it caught a prime hand-derivation error *in the same round that proposed the fix* — the harness measured the flag proposal's designed delta before the wrong budget formula could enter the frozen document.

## Decisions
1. **Φ(PENDING) fork PROPOSED-DISSOLVED (owner ratifies): PENDING is a FLAG on HOLDING (`committed=True`), not a stage.** Verified three ways: (a) PBRS-clean — no stage change → no eis reset → no minted climb; B's PBRS path ≡ an A-path committed at the boundary; (b) the r9 measurement-cadence bridge assumption discharges mechanically (the ablation predicate is stage-based; a flagged-HOLDING seed stays in `active_slot_list` — `vectorized_trainer.py:967-978`); (c) zero new machinery (no enum/potential entry; the holding_warning suppression already keys on `committed=True`). Both dead options recorded in §2.5.9.
2. **claude-prime's "−0.09·d ≈ −0.5…−0.9" designed-delta budget SUPERSEDED BY MEASUREMENT** (the round's own recommendation, corrected by the instrument it endorsed): the harness computes the flag proposal's B−A PBRS delta exactly per scenario — **|Δ| ≤ ~0.14 at d=5–10, sign varies with request dwell** (+0.138 at dwell5/d=5; −0.022 at dwell7/d=10; pre-cap flagged seeds keep climbing; fossils pay permanently higher cap-drag). Δ_designed(x) carries the harness value; NO hand budget formula enters the frozen doc. Prime hand-derivation tally: six instrument-corrected claims.
3. **PBRS reframe folded in:** the shipping layer is a monotone progress-payment stream (never Ng-invariant live); §2.5.9's "telescoping verified by replay" WITHDRAWN as stated; G-PBRS gate rewritten to production skip-semantics (within-stage identity + priced skips + single-climb-under-flag case); layer joins the known-non-invariant shaping ledger — matched across arms, **price-don't-fix mid-arc** (fix = post-experiment product defect, owner-gated).
4. **Gates re-postured as BOUNDS (PROPOSED):** tercile σ_d inflation ⇒ detection-grade n ≈ 29–63 in the estimation-error-dominated case — beyond any authorizable stretch ⇒ GATE-DOM/GATE-INFL scored as 95% UCB bounds vs Δ_material; wide bound at screen = INCONCLUSIVE-on-gates, pre-registered; a null is never a pass. Collapses the §8 "80/90 split" and "stretch" questions; **makes the r9 q_decision-IQR read (esper-lite-e12e2d1543) load-bearing for the gates, not just P2.**
5. **Conventions pinned:** forfeited-stream figure — Δ_designed uses the replay convention (discounted-at-request per-scenario PV; −274.7 at c=3/dwell5/T=150); the banked −113 is the realized r9 MC contrast, a different object. F8 (+45.8 ≈ 30× premium/event) cited in GATE-INFL's interpretation notes; holding_warning relief (+27.7) recorded as validating the §2.5.9 suspension clause.
6. **Prime ordering disagreement resolved by construction:** claude-prime wanted fold-in-first, gpt-prime owner-calls-first — the fold-in carries every disputed item as PROPOSED with dead options recorded, so the owner ratifies on the integrated document (the arc's standing pattern).

## Reversal trigger
- If the Phase-2 replay's request→boundary-under-flag case shows anything other than exactly ONE climb at the boundary → the dissolution's PBRS-clean argument fails; re-open the fork with the replay output as the new evidence base.
- If the flag construction is found to leak `committed=True` into any OTHER reward/observation path not enumerated in §2.5.9 (beyond warning suppression + slot-head masking) → the "no new machinery" claim fails; re-derive.
- If the q_decision-IQR read yields a Δ_material the gate bounds can never clear at n=10 → the bounds posture yields INCONCLUSIVE-on-gates by construction; escalate scope/budget to the owner BEFORE freeze (do not freeze a design whose safety instrument is known-uninformative).
