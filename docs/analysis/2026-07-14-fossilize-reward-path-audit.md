# FOSSILIZE reward-path audit (freeze blocker #2, rounds 15–17)

Date: 2026-07-14. Required by the round-15 owner adjudication ("trace the complete FOSSILIZE reward path") before any freeze. Source of truth: `src/esper/simic/rewards/contribution.py` + `rewards.py` + `shaping.py` + `leyline/__init__.py`, read at `5ca2a04f`. Scope: **SHAPED mode** (the experiment regime; A/B/C all run SHAPED per §1.3). Invariant under test (gpt-prime): *"No contribution measurement available at, or selected by, the request instant may enter an immediate FOSSILIZE payment."*

## 1. The complete ledger

### At the commit instant (FOSSILIZE action, valid contributing HOLDING seed)
| # | Term | Value | c-dependence at request instant | Where |
|---|---|---|---|---|
| 1 | Per-step provisional attribution | `contribution_weight(1.0)·attributed(c_spot)·discounts` | **spot-c-graded** but action-independent (a WAIT step pays the same) → not a FOSSILIZE-differential incentive. Zeroed if lifetime counterfactual < 0. | `contribution.py:569-601,644-654` |
| 2 | Flat base bonus | `fossilize_base_bonus 0.5 × legitimacy` | **spot-c-GATED**: paid only if `c_spot ≥ DEFAULT_MIN_FOSSILIZE_CONTRIBUTION (1.0)` | `:207, shaping helper` |
| 3 | Graded bonus | `0.1 × c_spot × legitimacy` | **spot-c-graded** (the known sell-at-spike term) | `:208` |
| 4 | "Immediate terminal" bonus | `3.0 × tanh(1/3) × legitimacy ≈ 0.964 × legitimacy` | **CONSTANT in magnitude** — `tanh(1.0/quality_ceiling)`, NOT `tanh(c)`. But **spot-c-GATED** (same `c_spot ≥ 1.0` + counterfactual ≥ 0 gate) | `:849-863` |
| 5 | Transaction cost | `fossilize_cost −0.01` | none | `:202` |
| 6 | **PBRS progress-forfeiture** (realized next step, HOLDING→FOSSILIZED) | `0.3·(0.995·6.0 − (5.5 + min(dwell·0.3, 2.0)))` = **−0.309 at dwell 5, −0.459 at dwell ≥7**; positive only for dwell ≤1 | dwell-graded, not c-graded | `_contribution_pbrs_bonus`, `STAGE_POTENTIALS` |

`legitimacy = min(1, epochs_in_HOLDING / MIN_HOLDING_EPOCHS(5))`. Penalty branches: invalid (no seed / not HOLDING / counterfactual None) → **−1.0**; counterfactual < 0 → **−0.5 − min(|cf|·0.2, 1.0) − 0.3·ransomware** (counterfactual-graded at the instant); `c_spot < 1.0` → **−0.2** (`fossilize_noncontributing_penalty`).

### The stay-in-HOLDING branch (the alternative being priced against)
Per-epoch: provisional attribution ~`c` (the ~3/epoch for good seeds) **minus** `holding_warning` **−0.1→−0.3/epoch** (non-terminal actions in HOLDING, `epochs_in_stage ≥ 2` ∧ `bounded_attribution > 0` — an anti-park penalty that fires exactly on GOOD seeds) **plus** PBRS progress accrual ≈ +0.08/epoch until the 2.0 cap (dwell ~7), then ≈ −0.01/epoch; occupancy rent above `free_slots=1`.

### Post-commit, per epoch until horizon
Attribution → **0** (frozen; the H4 fact); `fossilized_rent` **−0.002/epoch**; fossils still count in `n_occupied` (no occupancy escape); global terminal `val_acc × 0.05` at `max_epochs` (fossils contribute only causally via val_acc). **Drip** (70% pool, per-epoch keyed on *current* counterfactual): **BASIC_PLUS ONLY — inactive in SHAPED**; must stay mode-fenced from the settlement annuity (no coexistence path).

## 2. Findings

- **F1 — The "tanh contribution bonus" was mischaracterized in rounds 15–17.** It is constant-magnitude (`3.0·tanh(1/3) ≈ 0.964`), not contribution-scaled. It is spot-c-*gated*, not c-*graded*.
- **F2 — The shipping flat commit prior is ≈ 1.464 × legitimacy, not 0.5.** Terms 2+4 are BOTH flat and both live in arm A. The owner's matched-control rule ("retain exactly the existing flat premium; remove every contribution-dependent payment"), applied to the audited ledger, keys on **0.5 + 0.964 ≈ 1.464** — deleting the tanh term as "contribution-dependent" would itself be an intervention (halving the existing flat prior), the exact mistake the matched-control frame exists to prevent. **Recommendation: consolidate both flat terms into a single flat premium = 0.5 + 3.0·tanh(1/3) ≈ 1.464 (× legitimacy), remove only the 0.1·c grading.** One-line owner confirm needed (the adjudication said "+0.5" on the belief the tanh term was c-scaled).
- **F3 — The eligibility gate violates the invariant even after `0.1·c` dies.** `c_spot ≥ 1.0` at the request instant makes the *whole* ≈1.464 payment a binary function of the spot LOO — a threshold-crossing sell-at-spike channel (spike above 1.0 → collect; dip below → −0.2). ~~Fix: re-key the gate (and the −0.2 branch) from `c_spot` to `q_decision`~~ **[RETRACTED BY ADDENDUM A1 — `q_decision` is available at the request instant, so a payment gated on it remains policy-timeable; the compliant home is the boundary, keyed on `q_settle`.]**
- **F4 — PR28 CONFIRMED.** Additional c/counterfactual-keyed commit-time terms beyond the three known: (a) the threshold gate on both flat bonuses (F3); (b) the `−0.2` noncontributing branch (spot-c-keyed); (c) the counterfactual-graded invalid branch (`−0.5 − |cf|·0.2 − ransomware`); (d) the bounded-attribution zeroing keyed on lifetime counterfactual at the instant.
- **F5 — NEW, unpredicted: PBRS progress-forfeiture is a dwell-graded commit-time penalty (−0.31…−0.46 at legitimate dwells).** Committing forfeits HOLDING's accrued progress potential. Settlement interaction: the request→boundary window lengthens dwell → deeper forfeiture at the boundary transition → **G-LEDGER must include the PBRS potential path**, and the boundary transition's PBRS must be priced into the ledger-continuity check.
- **F6 — Round-15's dominance ledger missed the stay-branch penalty.** Stay is not pure `c·Σγ^k`: good seeds pay −0.1→−0.3/epoch indecision. Small vs ~3/epoch attribution, but it belongs in any B−A ledger statement.
- **F7 — Corrected fixed ledger (fully-legitimate commit, dwell 5, ~75 epochs remaining):** `+1.464 − 0.01 − 0.309 − PV(0.002/epoch ≈ 0.125)` ≈ **+1.02**. Not gpt's ≈+0.37 (0.5-only arithmetic) and not round-15's net-negative (zeroed-everything arithmetic). The −113-scale forfeited attribution stream remains the dominant term; that is the annuity's job.
- **F8 — The negative-carry escape hatch is LIVE in the shipping reward (round-17 §3(i) is not hypothetical).** A declining seed with lifetime counterfactual ≥ 0 but spot `c < 1.0` (including c < 0) can FOSSILIZE for a one-shot −0.2, then pay 0 attribution forever — vs staying and bleeding `c<0` per epoch. "Commit the decliner to stop the bleeding" is priced IN today. The settlement must apply the identical sign/clip transform through the annuity (negative `q_settle` → negative annuity) or mask requests at negative `q_decision`; do NOT rely on the behavioral gate to catch it (gates are one-sided in B).

## 3. Invariant verdict
**FAILS as shipped, four ways** (0.1·c grading; spot-c threshold gate on ≈1.464; spot-c −0.2 branch; instant-counterfactual-graded penalties). **Compliant form:** single flat premium ≈1.464×legitimacy, eligibility + penalty branches re-keyed to `q_decision`, validity re-keyed to the settlement's FRESH-staleness requirement, all c-graded *payment* moved to the policy-unselectable `q_settle` annuity, premium + maintenance-start paid at the **boundary** (not the request), PBRS boundary transition priced in G-LEDGER.

PR29 note: negative-LOO share ~10–12% (round-1 banked) makes F8 material, not a corner case.

---

## 4. ADDENDUM — rounds 18–19 adjudication (both primes; corrections to this audit's own dispositions)

**A1 — F3's recommended fix was WRONG; corrected to boundary-home.** Re-keying the eligibility gate to `q_decision` violates the invariant's own letter — `q_decision` is *available at the request instant* by definition, so a binary ≈1.464 payment gated on it is a threshold instrument on a smoothed observable (dwell until the EWMA crosses 1.0, then request — slower than the spot channel, same class; READ 1's verdict was about the class). An action-mask implementation would additionally bake the quote→request correlation into P2 mechanically. **Compliant disposition (three payment instants):** at REQUEST — no contribution-conditioned payment of any sign (the `0.1·c`, spot-c gate, −0.2 branch, and counterfactual-graded penalties are all removed from this instant); charge only `fossilize_cost=−0.01`; freeze `legitimacy_request`. During the WINDOW — provisional stream runs; no new payments. At the BOUNDARY — the fixed prior `1.464538 × legitimacy_request`, **discount-neutral** (`÷γ^d`; index convention unit-tested), gated on `q_settle ≥ 1.0`, with the sign-symmetric −0.2 branch also `q_settle`-keyed; signed annuity begins; maintenance starts. "Amount-matched, boundary-qualified fixed commitment prior." Arm A's shipping semantics are untouched; B−A is the protocol-package contrast (already the declared estimand).

**A2 — F5 RECLASSIFIED: code fact, economic classification OPEN.** A negative per-transition PBRS term is not automatically a cost: the potential is a state function (stage + `epochs_in_stage`), so the discounted stream should telescope to `γ^T·Φ(s_T) − Φ(s_0)`. Note the truncation residual is NOT negligible here: episodes truncate at `max_epochs=150` with `γ^150 ≈ 0.47`, and `Φ(FOSSILIZED)=6.0` vs `Φ(PRUNED)=0` — the whole-path shaping may even be net pro-fossil. **Do not refund −0.459; do not absorb PBRS into the prior.** Required by the replay: does the stream telescope end-to-end (clips? terminal handling? stage-dependent exceptions)? What potential does the PENDING state carry (to be DEFINED, then verified)? Does total discounted PBRS depend on window length or audit-cycle phase?

**A3 — `holding_warning` window condition: READ ANSWERED (`contribution.py:706-717`).** Keying: targeted seed `stage == STAGE_HOLDING` ∧ action ∉ {FOSSILIZE, PRUNE} ∧ `epochs_in_stage ≥ 2` ∧ `bounded_attribution > 0`. The reward is per-decision, so during the window it fires **only when the policy targets the pending slot with a non-terminal op** (e.g. WAIT on it) — NOT automatically every epoch. Round-19's −1.5 assertion was an over-read (self-retracted); the true magnitude is `−0.1…−0.3 × P(target pending slot with non-terminal op)` — nonzero, unpriced, and state-dependent. **Spec clause (landed in pre-reg §2.5.9):** `holding_warning` is suspended for `committed=True` seeds — the request ends indecision, so it must end indecision-priced penalties. The pending state must be semantically distinct in reward logic.

**A4 — F7 RELABELED: "illustrative fixed-term balance at dwell 5."** It omits avoided future holding warnings, the PBRS path, window effects, occupancy deltas, signed provisional attribution, the annuity, payment timing/discount, threshold eligibility, and terminal/truncation. The net-negative / +0.37 / +1.02 sequence differed because each answered a different partial-ledger question. No dominance-over-WAIT claims from any single scalar — the executable replay is the instrument.

**A5 — The window is a THIRD reward regime; its ledger is a READ QUEUE, not findings:** provisional stream (runs), PBRS (PENDING Φ to define; telescoping to verify), `holding_warning` (suspend — A3), occupancy rent (runs; real occupancy), maintenance (starts at boundary), + PR32 candidate: `epochs_since_counterfactual` staleness interaction with the frozen-α window when the solo-eval rotation skips a mid-window seed.

**A6 — NEW FREEZE BLOCKER: executable G-LEDGER replay.** Hand arithmetic found the terms; it cannot certify their transition semantics. Run the production SHAPED reward path over the case matrix — contribution {+const, 0, −const} × dwell {1, 5, 7, post-cap} × audit-cycle phase (all, W=10) × `q_settle` {<1, =1, >1} × window {min, max, **missed-measurement**} × horizon {ordinary, shortest-legal, terminal-edge, truncation} × status {fresh, stale, never} × lifecycle {normal, prune/reuse, generation-turnover} × multi-seed {1, several pending/annuity ledgers, **adversarial neighbor-op sequences**} — printing per-component discounted PV (provisional, holding_warning, PBRS, prior, request-time bonuses, cost, occupancy, maintenance, annuity, terminal, total). *(Invariant statements superseded by A7's corrected contract.)*

**A7 — Round-20 corrections to the replay contract (adopted).**
- **The A6 "invariant to dwell" statement was impossible as written** — the prior scales with `legitimacy(dwell_at_request)` by design. Corrected form: `residual = Δ_observed − Δ_designed(x) ≈ 0`, where `x` = (request dwell + frozen legitimacy, request→boundary delay d, eligible-vs-noncontributing branch, remaining horizon, maintenance/occupancy, transaction costs, PBRS endpoint residual, preserved warning semantics). The **residual** must be invariant to audit-cycle phase, irrelevant spot noise, and implementation accident; the **designed delta** may legitimately vary with dwell/branch/horizon exactly per the frozen formula.
- **Three comparator trajectories, not two:** S (stay provisional), A (shipping immediate FOSSILIZE), B (protocol request→window→boundary). Gates: **G-CONTINUITY** (B−S, contribution-accounting stream ONLY — provisional through window + signed annuity after ≈ stay stream; +/0/− constant c; excludes prior/cost/maintenance/occupancy/PBRS/holding_warning/task reward); **G-MATCHED-CONTROL** (B−A — prior PV, both fixed branches, cost, `holding_warning` cessation parity [arm A's action ends indecision at the instant → B must too; this parity is demanded of B−A, NOT B−S], PBRS semantics, maintenance start); **G-DESIGNED-DELTA** (B−S full = `Δ_designed(x)`, residual ≈ 0); **G-PBRS** (whole-path telescoping incl. request/pending/boundary/terminal/truncation/PRUNE); **G-EVENT-ORDER** (half-open intervals: request epoch t pays provisional once + −0.01; pending t+1…b−1 provisional; boundary b begins annuity+maintenance and pays the prior; no epoch receives both provisional and annuity, none receives neither); **G-CROSS-SLOT** (adversarial multi-slot sequences — see the construction decision in pre-reg §2.5.7); **G-THRESHOLD-VARIANCE** (below). PASS = all seven.
- **Every fixed branch moved from request to boundary is discount-neutralized, both signs** (`R_boundary = R/γ^d`) — including the −0.2 noncontributing branch (unneutralized it decays with d → an audit-phase incentive). Equality asserted on **raw pre-normalization reward**; post-clip and post-adaptive-normalizer values are REPORTED as measured downstream distortion (path-dependent normalizers cannot be held to exact equality), never folded into the raw invariant.
- **G-THRESHOLD-VARIANCE (new zero-GPU test):** the `q_settle ≥ 1.0` cliff is option-like — equal-mean/different-variance quote histories with means just below/at/above 1.0 must show no material positive expected windfall from variance alone. If they do → qualify on a robust policy-unselectable statistic (`LCB(q_settle) ≥ 1`), never back to a `q_decision` gate.
- **Premium frozen as the EXPRESSION `0.5 + 3.0·tanh(1.0/3.0)` evaluated in-code** (decimal 1.464538… printed once from the interpreter into the record) — a hand-rounded literal can trip the replay's own ~1e-6 tolerance against the shipping float64 value.
- **PENDING potential — default `Φ(PENDING) := Φ(FOSSILIZED) = 6.0`** (gpt): the irreversible decision happens at the request, so the PBRS transition (and the dwell-progress forfeiture) is realized at the ACTION instant — matching arm A's immediate-FOSSILIZE semantics (G-MATCHED-CONTROL parity) and leaving no phase-dependent shaping cliff at the boundary. Recorded alternative (claude): `Φ(PENDING) := Φ(HOLDING, dwell_at_request)` frozen — rejected as default because it defers the forfeiture to the schedule-timed boundary (PV varies with d → phase leak) and mismatches A at the instant. The replay's phase/window-length-dependence case adjudicates whichever is implemented.
- **Derived residual (claude, executed read of §1's own table): `Φ(HOLDING, dwell-capped)=7.5 > Φ(FOSSILIZED)=6.0` — the potential ranks a PARKED seed above a FOSSIL.** On the decision-relevant contrast (fossilize vs park-to-horizon) the shaping residual pays parking ≈ `0.3·γ^{T−t}·1.5` ≈ +0.31 at the median fossilize epoch — the park-over-permanence preference encoded in the shaping term itself. A2's "possibly pro-fossil" compared fossil vs PRUNED (wrong pair). Shared across arms → B−A unconfounded; ~25% haircut on the designed net, sign unchanged. Disposition: do NOT touch Φ mid-experiment (matched-control); carry the differential explicitly in `Δ_designed`; name it as a suspect in GATE-DOM's interpretation notes (if B's commit preference stays suppressed, this is one candidate).
